<%- include('layout', { body: '' }) %>
<h2>Dashboard</h2>
<div class="row">
  <div class="col-md-4">
    <div class="card mb-3">
      <div class="card-body">
        <h5>Total Products</h5>
        <p class="display-6"><%= stats.countProducts %></p>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card mb-3">
      <div class="card-body">
        <h5>Total Customers</h5>
        <p class="display-6"><%= stats.countCustomers %></p>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <div class="card mb-3">
      <div class="card-body">
        <h5>Today Revenue</h5>
        <p class="display-6">$<%= stats.todayRevenue %></p>
      </div>
    </div>
  </div>
</div>

<div class="row mt-3">
  <div class="col-md-6">
    <div class="card-nike">
      <h5>Low Stock Alerts</h5>
      <% if (!lowStock || !lowStock.length) { %>
        <p class="mt-2">No low-stock products 🎉</p>
      <% } else { %>
        <ul class="mt-2">
          <% lowStock.forEach(p => { %>
            <li><strong><%= p.name %></strong> — <span style="color:#b00020;"><%= p.quantity_on_hand %> left</span></li>
          <% }) %>
        </ul>
      <% } %>
    </div>
  </div>
</div>
